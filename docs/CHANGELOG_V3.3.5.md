# CHANGELOG v3.3.5

## 공백이 포함된 Riot ID 조회 수정

`URLEncoder`는 공백을 `+`로 인코딩합니다.

예:
- 기존: `이세계+펭구`
- 수정: `이세계%20펭구`

Riot Account API의 URL 경로에서는 공백을 `%20`으로 전달해야 하므로
`enc()` 메서드에서 `+`를 `%20`으로 교체하도록 수정했습니다.
