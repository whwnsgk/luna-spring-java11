# CHANGELOG v3.4.6

- 메인 이미지 경로를 `main-image.png`로 변경
- 별도 메인 이미지 섹션 제거
- PNG 이미지를 영상 영역 좌측 상단 카메오 형태로 표시
- 친구 명언을 메인 히어로에 추가
- 영상은 기존처럼 `main-video.mp4`를 전체 배경으로 재생
- 영상 파일이 없으면 버튼에 `영상 파일 없음` 표시
- 기존 어워드/통계 영역은 유지
